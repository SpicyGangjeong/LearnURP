---
description: Agent worktree — read worktrees/state.json
alwaysApply: true
---

# Worktree

- Code/commits: `C:\Users\golde\OneDrive\Desktop\LearnURP-cursor` (branch `HandManage`)
- State file: `worktrees/state.json` (v1 compact JSON — deserialize directly)
- Refresh git snapshot: `.\scripts\worktree.ps1 scan`
- Update agent progress: `set-prog -Role agent -Task ... -Wip ... -Todo ...` or `load -InFile patch.json`
- User glance: `.\scripts\worktree.ps1 show`

Schema: `worktrees/schema.json`
